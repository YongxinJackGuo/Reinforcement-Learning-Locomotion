import torch

class Value_Net():
    def __init__(self, ob_dim, value_dim, hid_layers):
        if value_dim is None: value_dim = 1
        if hid_layers is None: hid_layers = [128, 128]

        # Define the network architecture



    def forward(self, obs):

        return None
